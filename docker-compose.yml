networks:
  ocean_network:
    name: ocean_network
    external: true


services:
  ocean_db_client:
    image: ocean_db_client:latest
    stdin_open: true
    tty: true
    env_file:
      - .env
    volumes:
      - .:/app/OceanDB
    networks:
      - ocean_network
